% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simLongData.R
\name{simLongData}
\alias{simLongData}
\title{Simulate Longitudinal Time Series Data for Clustering}
\usage{
simLongData(
  n_total = 200,
  K = 4,
  outcomes = 5,
  eta = 3,
  cluster_sizes = rep(n_total/K, K),
  ranTimes = TRUE,
  n_i = 12
)
}
\arguments{
\item{n_total}{Integer. Total number of subjects to simulate. Default is 200.}

\item{K}{Integer. Number of underlying clusters to simulate. Default is 4.}

\item{outcomes}{Integer. Dimensionality of the time series (number of outcomes per time point). Default is 5.}

\item{eta}{Numeric. Noise level in the data. Default is 3.}

\item{cluster_sizes}{Numeric vector. Number of subjects in each cluster. Should sum to \code{n_total}.
Default is equal cluster sizes.}

\item{ranTimes}{Logical. If \code{TRUE}, simulate random time points per subject; otherwise, use fixed time grid.
Default is \code{TRUE}.}

\item{n_i}{Integer. Number of fixed time points per subject (only used if \code{ranTimes = FALSE}). Default is 12.}
}
\value{
A data frame with the following columns:
\describe{
  \item{\code{subject}}{Integer. Subject ID (1 to \code{n_total}).}
  \item{\code{time}}{Numeric. Time point at which the observation was recorded.}
  \item{\code{outcome}}{Integer. Outcome dimension (1 to \code{outcomes}).}
  \item{\code{y}}{Numeric. Observed value of the time series at given time and outcome dimension.}
  \item{\code{cluster}}{Integer. True cluster assignment for the subject.}
}
}
\description{
Generates multivariate longitudinal data with a specified number of clusters,
noise level, and time structure. Can simulate either fixed or random observation times.
}
\examples{
sim_data <- simLongData()
head(sim_data)

}
