% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_random_curve.R
\name{generate_random_curve}
\alias{generate_random_curve}
\title{Generate Subject-Specific Random Curves}
\usage{
generate_random_curve(times, sigma_b = diag(c(2, 0.3, 0.06)), rho_b)
}
\arguments{
\item{times}{A numeric vector of time points at which to evaluate the subject-specific random curve.}

\item{sigma_b}{A 3x3 diagonal variance matrix (default: \code{diag(c(2, 0.3, 0.06))}) specifying the marginal variances of the random coefficients \eqn{b_{i0}}, \eqn{b_{i1}}, and \eqn{b_{i2}}.}

\item{rho_b}{A 3x3 correlation matrix specifying the correlation structure among the random coefficients, as defined in Equation (10) of Zhou et al. (2022).}
}
\value{
A numeric vector of the same length as \code{times}, representing the subject-specific time effect evaluated at each time point.
}
\description{
Computes random subject-level time effects based on a quadratic random effects model:
\eqn{r_i(t) = b_{i0} + b_{i1} t + b_{i2} t^2}, as defined in Equation (9) of Zhou et al. (2022).
}
\details{
This function is used internally by \code{\link{simLongData}} to generate smooth, individual-specific
deviations from cluster-level means.
}
\examples{
times <- seq(0, 1, length.out = 10)
rho_b <- diag(3) # identity correlation matrix for simplicity
curve <- generate_random_curve(times, rho_b = rho_b)

}
\references{
Zhou, J., Zhang, Y., & Tu, W. (2022). clusterMLD: An Efficient Hierarchical Clustering Method for Multivariate Longitudinal Data. *Journal of Computational and Graphical Statistics*, DOI: \doi{10.1080/10618600.2022.2149540}
}
